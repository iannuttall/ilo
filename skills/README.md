# Agent skill

This package ships the `ilo` skill. It teaches agents to use the local CLI and MCP server safely, especially around publishing confirmation.

Install it with:

```sh
ilo skill install
```

The same skill is included in the npm package and this repository.
