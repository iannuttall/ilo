# ilo core

Shared local implementation for the ilo CLI, MCP server, and public TypeScript package. It owns X providers, resumable public follower and article imports, stored profile and article search, drafts, scheduling, publishing, SQLite storage, and keychain-backed credentials.
